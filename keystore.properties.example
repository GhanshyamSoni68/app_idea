# Copy to keystore.properties (which is gitignored) and fill in before building
# a release. Without this file the release build simply goes unsigned, so debug
# builds keep working on a fresh clone.
storeFile=/absolute/path/to/expiry-release.jks
storePassword=
keyAlias=expiry
keyPassword=
